generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://myuser:mypassword@localhost:5432/getclip"
}

model User {
  id    Int    @id @default(autoincrement())
  name  String
  email String @unique
}
